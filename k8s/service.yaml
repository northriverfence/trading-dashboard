apiVersion: v1
kind: Service
metadata:
  name: stock-agent
  namespace: stock-agent
  labels:
    app: stock-agent
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: stock-agent
